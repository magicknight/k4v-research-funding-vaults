{
  "name": "k4v-research-funding-vaults",
  "version": "0.1.1",
  "private": true,
  "license": "MIT OR Apache-2.0",
  "description": "Reproducible purpose-bound token vault reference models and Solana probes",
  "scripts": {
    "check": "node --check probes/solana_fixed_supply_probe.cjs && node --check probes/squads_authority_probe.mjs && node --check probes/b2_devnet_probe.mjs && node --check probes/b2_devnet_verify.mjs && node --check probes/r3_full_scale_squads_probe.mjs && node --check probes/r3_full_scale_squads_verify.mjs"
  },
  "devDependencies": {
    "@solana/spl-token": "0.4.15",
    "@solana/web3.js": "1.98.4",
    "@sqds/multisig": "2.1.4"
  }
}
